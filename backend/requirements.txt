# --- Core web stack (required to boot the API) ---
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.9
pydantic-settings>=2.5
python-multipart>=0.0.12
httpx>=0.27

# --- LLM (optional at runtime: orchestrator falls back to an offline engine
#     if the SDK or an API key is missing) ---
anthropic>=0.40

# --- Data-analysis / sandbox runtime (used by the Analysis Service and the
#     Sandbox Manager's execution image; profiling degrades gracefully if
#     these are unavailable on the host) ---
numpy>=1.26
pandas>=2.1
scipy>=1.11
matplotlib>=3.8
statsmodels>=0.14
openpyxl>=3.1
duckdb>=1.1

# --- deep-capability extras ---
trafilatura>=1.12            # clean web-page text extraction (deep_research)
pypdf>=4.0                   # PDF text extraction (ingestion pipeline)

# --- production infrastructure ---
psycopg[binary]>=3.2         # PostgreSQL driver (postgresql+psycopg://)
pgvector>=0.3                # pgvector helpers
celery[redis]>=5.4           # background jobs (ingestion, heavy analysis)
redis>=5.0                   # broker + rate-limit backend

# --- observability (OpenTelemetry) ---
opentelemetry-sdk>=1.27
opentelemetry-instrumentation-fastapi>=0.48b0
opentelemetry-exporter-otlp-proto-http>=1.27

# --- tests ---
pytest>=8.0

# NOTE ON DEPENDENCIES WE INTENTIONALLY DO NOT PULL IN:
#   - No LangChain/LlamaIndex   -> custom orchestrator (architecture 6.1)
#   - No passlib/bcrypt/PyJWT   -> auth crypto on the stdlib (scrypt + HS256)
#   - No Vite, anywhere         -> frontend uses Next.js' own bundler
